{
  "name": "replace_layers",
  "version": "1.1",
  "description": "Replace Layers [Beta] allows you to select one or more layers (aka nodes) and replace them with an existing layer in your Figma document.",
  "main": "code.js",
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "author": "",
  "license": "",
  "devDependencies": {
    "@figma/plugin-typings": "^1.16.1"
  }
}
